# 백엔드 API URL (런타임에 nginx 프록시 대상으로 사용)
# 같은 docker compose 내: http://backend:8080 (기본값)
# 외부 서버: http://your-server-ip:8080
API_BACKEND_URL=http://backend:8080

# CORS 허용 오리진 (기본값: http://localhost)
# 여러 개: http://localhost,http://example.com
CORS_ALLOWED_ORIGINS=http://localhost
